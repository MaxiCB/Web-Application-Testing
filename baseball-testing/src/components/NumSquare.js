import React from 'react';

import { Card, CardTitle } from 'reactstrap';

const NumSquare = (props) => {
    return (
        <Card body style={{backgroundColor: 'black', border: '5px solid #333'}}>
            <CardTitle>
                0
            </CardTitle>
        </Card>
    )
}

export default NumSquare;